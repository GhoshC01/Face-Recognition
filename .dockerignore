.git/
.gitignore
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/

tests/
docs/
*.md

.env
.env.*
!.env.example

# Model binaries and persisted state are mounted/volumed at runtime, never
# baked into the image or sent as part of the build context.
models/*.onnx
storage/faiss/*.faiss
storage/metadata/*.json
